diff --git a/src/large.py b/src/large.py
@@ -1,6 +1,36 @@
 def process(items):
-    total = 0
-    for item in items:
-        total += item
-    return total
+    total = 0
+    # expanded processing
+    for item in items:
+        if item is None:
+            continue
+        total += item
+    for item in items:
+        if item is None:
+            continue
+        total += item
+    for item in items:
+        if item is None:
+            continue
+        total += item
+    for item in items:
+        if item is None:
+            continue
+        total += item
+    for item in items:
+        if item is None:
+            continue
+        total += item
+    for item in items:
+        if item is None:
+            continue
+        total += item
+    for item in items:
+        if item is None:
+            continue
+        total += item
+    return total
